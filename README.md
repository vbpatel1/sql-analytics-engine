# sql-analytics-engine
A tool that ingests book and author data from the Open Library API, stores it in Postgres, and lets users query it — either through a CLI, a REST API, or by typing a plain English question that gets converted to SQL via Claude. 
